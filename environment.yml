name: agabgated
channels: [pytorch, conda-forge, defaults]
dependencies:
  - python=3.10
  - numpy=1.26
  - pandas=2.3
  - scipy=1.15
  - matplotlib=3.10
  - scikit-learn=1.6
  - pip
  - pip:
      - torch==2.2.1
      - transformers==4.47.1
      - captum==0.8.0
      - biopython==1.85
      - ANARCI==2026.2
      - tqdm
      - pyyaml
